# Ragas evaluation report

**Skipped.**

GROK_API_KEY is not set — Ragas needs an LLM to judge faithfulness and answer relevancy. Set it and re-run.

This is expected in a zero-config environment — see the project README's "runs with no API keys" quickstart. Set `GROK_API_KEY` (and run `pip install -e ".[eval]"`) for a live evaluation.